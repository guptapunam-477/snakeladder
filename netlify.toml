# Netlify configuration for Chaos Ladder (Vite SPA)
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every route is served by index.html so deep links
# like /room/ABC123 work after a refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
